<div class="row">
  <% @likes.each do |like| %>
    <div class="col-md-6 col-md-offset-3">
      <ul class="list-group">
        <li class="list-group-item"><%= like.user.name %>:<%= like.content %></li>
      </ul>
    </div>
  <% end %>
</div>
